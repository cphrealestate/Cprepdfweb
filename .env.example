# Sanity CMS Configuration
# Get these values from https://sanity.io/manage

# Your Sanity Project ID
VITE_SANITY_PROJECT_ID=your_sanity_project_id_here

# Sanity Dataset (usually 'production')
VITE_SANITY_DATASET=production

# Vercel Deploy Hook (for automatic rebuilds when Sanity content changes)
# Get this from Vercel Dashboard → Settings → Git → Deploy Hooks
# Example: https://api.vercel.com/v1/integrations/deploy/prj_xxx/xxx
VERCEL_DEPLOY_HOOK=your_vercel_deploy_hook_url

# Optional: Webhook security secret
# Generate a random string for webhook signature validation
SANITY_WEBHOOK_SECRET=your_random_secret_here

# Instructions:
# 1. Copy this file to .env.local (for local development)
# 2. Replace the placeholder values with your actual credentials
# 3. Add these same variables to Vercel Dashboard → Settings → Environment Variables
# 4. NEVER commit .env.local to git (it's already in .gitignore)
